[pytest]
testpaths = tests
pythonpath = .

# The interactive viewer blocks on a human, so it is out of every run unless asked for by
# `-m interactive`.
addopts = -m "not interactive"

markers =
    interactive: opens a blocking window and needs a person at the keyboard
